<br>
    <h1>Comments</h1>
<br>
<form action="<?php echo Mage::getUrl('comments/comments', array('product_id' => Mage::registry('current_product')->getId() )); ?>" method="post" id="summa_comment">

    <div class="input-box">
        <textarea rows="10" cols="50" id="comment" name="comment" class="input-text required-entry"></textarea>
        <input type="submit" value="Post">
    </div>


</form>

<?php foreach ($this->getCommentList() as $comment):?>
    <div class="product-collateral">
        <?php
            $user_id = $comment->getUserId();
        ?>
        <?php if (empty($user_id)):?>
            <span><b>Anonymous</b></span>
        <?php else: ?>
            <span><b><?php echo $this->getUserName($user_id)?></b></span>
        <?endif?>

        <span><?php echo $comment->getComment()?></span>

        <div>Posted:
            <span class="note">
                <b><?php echo Mage::helper('core')->formatDate($comment->getDate(), 'medium', false)?></b>
            </span>
        </div>
    </div>
<?php endforeach;?>
